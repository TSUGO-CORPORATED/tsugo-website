// MODULES IMPORT
import { Metadata } from 'next';

// PAGE NAME
export const metadata: Metadata = {
    title: 'Sign Up',
}

// PAGE COMPONENT
export default async function SignUp() {
    return (
        <div>Sign Up</div>
    )
}